import 'package:flutter/material.dart';

class FitnessTracker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Fitness Tracker')),
      body: Center(child: Text('Fitness Tracking Coming Soon!')),
    );
  }
}
